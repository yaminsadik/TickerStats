"""
Sector Invariants sub-modules.

Each module exports:
  build_context(inputs: dict) -> dict   — extract only relevant fields from inputs
  build_prompt_fragment(ctx: dict) -> str — LLM prompt fragment for the module
"""
