"""
Business Model & Segments sub-modules.

Each module exports:
  build_context(inputs: dict) -> dict   — pre-compute fallback-resolved data
  build_prompt_fragment(ctx: dict) -> str — LLM prompt fragment for the module
"""
